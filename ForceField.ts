/// <reference path="Vectors.ts" />

import Vector = Vectors.Vector;
import VectorPool = Vectors.VectorPool;

namespace ForceField {
    export interface IForceFieldComponent {
    }

    // export abstract class BaseForceFieldComponent implements IForceFieldComponent {
    //     abstract eval(items: IForceFieldComponent[], coords: Vector[], forces?: Vector[]): number;
    // }
}